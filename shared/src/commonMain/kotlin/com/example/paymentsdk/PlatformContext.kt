package com.example.paymentsdk

/**
 * Platform-specific context required by the SDK.
 *
 * - Android: `Activity` (needed to launch Custom Tabs)
 * - iOS: empty class (no context needed)
 */
expect class PlatformContext
