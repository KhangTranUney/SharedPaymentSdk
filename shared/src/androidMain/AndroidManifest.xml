<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        SDK-internal activities for in-house payment flow.
        Merged into the host app's manifest automatically.

        To customize the callback scheme (default: "myapp"),
        override paymentCallbackScheme in your app's
        build.gradle manifestPlaceholders.
    -->

    <application>

        <activity
            android:name="com.example.paymentsdk.inhouse.PaymentBrowserActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:exported="false" />

        <activity
            android:name="com.example.paymentsdk.inhouse.PaymentRedirectActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:scheme="${paymentCallbackScheme}"
                    android:host="payment"
                    android:pathPrefix="/callback" />
            </intent-filter>

        </activity>

    </application>

</manifest>
